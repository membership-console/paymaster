datasources:
  default:
    driverClassName: org.postgresql.Driver
    db-type: postgresql
    url: "jdbc:postgresql://localhost:55432/paymaster"
    username: "paymaster"
    password: "paymaster"
    schema-generate: none
micronaut:
  security:
    oauth2:
      clients:
        iam:
          client-id: "test"
          client-secret: "test"
          token:
            url: "test"
